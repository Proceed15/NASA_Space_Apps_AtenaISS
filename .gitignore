# Dependencies
# This folder is generated when you run 'npm install'
# and can be very large. It should always be ignored.
/node_modules

# Vite build and cache files
# These folders are created by Vite during development and when you build the project.
/dist
/.vite

# Log files
# These files are often created for debugging and can vary between machines.
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment variables
# This file contains sensitive information like API keys and passwords.
# NEVER commit this file to a public repository.
.env
.env.local
.env.*.local

# IDE and editor directories
.vscode/
.idea/

# OS-specific files
# These are system files created by macOS, Windows, or Linux.
.DS_Store
Thumbs.db
